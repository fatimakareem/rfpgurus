import { DatePickerComponent } from './datePicker.component';
describe('a input component', function () {
    var datePickerComponent;
    var mockDateService;
    // register all needed dependencies
    beforeEach(function () {
        mockDateService = jasmine.createSpyObj('mockDateService', ['getDateList', 'getMonths', 'getAvailableYears']);
        mockDateService.getDateList.and.returnValue([]);
        datePickerComponent = new DatePickerComponent({}, mockDateService, {});
    });
    it('should have an instance', function () {
        expect(datePickerComponent).toBeTruthy();
    });
    it('should set the selected date to today when no date is set', function () {
        datePickerComponent.ngOnInit();
        expect(datePickerComponent.selectedDate.getDate()).toEqual(new Date().getDate());
        expect(datePickerComponent.selectedDate.getMonth()).toEqual(new Date().getMonth());
        expect(datePickerComponent.selectedDate.getFullYear()).toEqual(new Date().getFullYear());
    });
    it('should hide the picker', function () {
        var visibility = false;
        datePickerComponent.closePicker(visibility);
        expect(datePickerComponent.pickerVisible).toBe(false);
    });
});
//# sourceMappingURL=C:/Code/ngx-datetimepicker/ngx-datetimepicker/src/app/datePicker.component/datePicker.component.spec.js.map